<% layout("./layouts/boilerplate") %>

  <h1>Updating Objects</h1>
  <p>We can update values of existing keys and create new keys easily.</p>
  <p>
    If the key does not exist yet, we can simply access the object followed by
    the new key and define it.
  </p>
  <p>If it already exists, the syntax is the same.</p>
  <h3>with dot notation</h3>
  <img src="updatingAndCreatingObjectKeys.png" alt="" />
  <h3>with bracket notation</h3>
  <img src="updatingObjectsBracketNotation.png" alt="" />