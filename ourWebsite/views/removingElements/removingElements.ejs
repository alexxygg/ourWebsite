<% layout("./layouts/boilerplate") %>

  <h1>Removing Elements</h1>
  <h2>removeChild()</h2>
  <p>This will remove the child of the parent element.</p>
  <p>We can do it in 2 ways:</p>
  <p>
    Either declare a variable for the parent and for the child elements...
  </p>
  <p>
    Or call the parentElement method on the declared child element and pass in
    the childElement into the parenthesis.
  </p>
  <img src="removeChildElementCode.png" alt="" />
  <img src="removeChildElement.png" alt="" />
  <p>
    There were 3 list items as in the second unordered list, but after
    removing 2 we were left with this.
  </p>
  <h2>remove()</h2>
  <p>This will remove the element directly!</p>
  <img src="remove.png" alt="" />