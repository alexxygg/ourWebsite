<% layout("./layouts/boilerplate") %>
  <h1> some() & every() Methods</h1>
  <p>These methods help us return true or false, they are boolean methods.</p>

  <h2>.some()</h2>
  <p>This method will return true if at least one item passes the test.</p>
  <p>It will only return false if NONE of the items pass.</p>
  <img src="someMethodCode.png" alt="" />
  <img src="someMethodConsole.png" alt="" />

  <h2>.every()</h2>
  <p>
    This method will check for our expression, and if every item passes the
    test, it will return true, otherwise false.
  </p>
  <img src="everyMethodCode.png" alt="" />
  <img src="everyMethodConsole.png" alt="" />

  <script src="someAndEveryMethod.js"></script>